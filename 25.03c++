#include <iostream>
using namespace std;

int main()
{
	const int N = 20;
	char str1[N], str2[N];
	cin >> str1 >> str2;

	char* res = strstr(str1, str2);
	if (res != nullptr) {
		for (int i = 0; i < strlen(str2); i++) 
		{
			*res = '*';
			res++;
		}
	}
	cout << str1;

	return 0;
}


